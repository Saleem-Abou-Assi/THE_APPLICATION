import React, { useEffect, useState } from 'react';
import { View, Text, Button, TextInput } from 'react-native';


const Main: React.FC = () =>{

    useEffect(()=>{

        const runMigrations = ()=>{
            runMigrations;
        } ;
    });

    return(
        <View>
            
        </View>
    );


};

export default Main;